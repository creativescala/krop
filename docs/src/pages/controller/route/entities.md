# Entities

In HTTP, the term "entity" refers to the body of a request, which holds data that is used by a handler. For example, a form submission or a JSON request will have form or JSON data in the entity, respectively.


## JSON

`Entity.jsonOf`


## Forms

`Entity.formOf`
